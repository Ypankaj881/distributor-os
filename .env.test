# Loaded AFTER .env.local by `npm test`, overriding only what's listed here.
# Tests use a separate database that is WIPED at the start of every test file.
# The test helper refuses to run unless the database name ends in "_test".
MONGODB_DB=distributor_os_test
DEFAULT_COMPANY_SLUG=test-co
